# dbt sources — per-dataset raw landing zones written by engine/ingest.py.
# Managed by `scaffold --write`; edit descriptions freely.
version: 2
sources:
- name: anage
  schema: anage_raw
  description: Raw landing tables for the anage dataset (engine/ingest.py).
  tables:
  - name: anage
    description: AnAge animal ageing & longevity records, exactly as downloaded (tab-delimited).
- name: nba
  schema: nba_raw
  description: Raw landing tables for the nba dataset (engine/ingest.py).
  tables:
  - name: games_index
    description: Raw games_index table.
  - name: games_schedule
    description: Raw games_schedule table.
  - name: team_boxscores
    description: Raw team_boxscores table.
  - name: player_boxscores
    description: Raw player_boxscores table.
