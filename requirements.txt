# ── Core data flow (no API keys, no cloud) ──────────────────────────────
duckdb>=1.1
dbt-core>=1.7
dbt-duckdb>=1.7
pyyaml>=6.0
sqlglot>=25.0
streamlit>=1.36
pandas>=2.0
altair>=5.0

# ── AI layers only (scaffold / orchestrate / ask) ───────────────────────
# The core `make up` pipeline runs green without these being configured.
anthropic>=0.40
pydantic>=2.6
python-dotenv>=1.0
