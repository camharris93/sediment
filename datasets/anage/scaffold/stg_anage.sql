-- GENERATED by engine/scaffold.py (deterministic baseline) — REVIEW ME.
-- 1:1 staging for raw.anage: snake_case rename only. Add casts, unit
-- standardization, and junk-row filters, then move into models/staging/.

with source as (
    select * from {{ source('raw', 'anage') }}
),

renamed as (
    select
        "HAGRID"                             as hagrid,
        "Kingdom"                            as kingdom,
        "Phylum"                             as phylum,
        "Class"                              as class,
        "Order"                              as order_,
        "Family"                             as family,
        "Genus"                              as genus,
        "Species"                            as species,
        "Common name"                        as common_name,
        "Female maturity (days)"             as female_maturity_days,
        "Male maturity (days)"               as male_maturity_days,
        "Gestation/Incubation (days)"        as gestation_incubation_days,
        "Weaning (days)"                     as weaning_days,
        "Litter/Clutch size"                 as litter_clutch_size,
        "Litters/Clutches per year"          as litters_clutches_per_year,
        "Inter-litter/Interbirth interval"   as inter_litter_interbirth_interval,
        "Birth weight (g)"                   as birth_weight_g,
        "Weaning weight (g)"                 as weaning_weight_g,
        "Adult weight (g)"                   as adult_weight_g,
        "Growth rate (1/days)"               as growth_rate_1_days,
        "Maximum longevity (yrs)"            as maximum_longevity_yrs,
        "Source"                             as source,
        "Specimen origin"                    as specimen_origin,
        "Sample size"                        as sample_size,
        "Data quality"                       as data_quality,
        "IMR (per yr)"                       as imr_per_yr,
        "MRDT (yrs)"                         as mrdt_yrs,
        "Metabolic rate (W)"                 as metabolic_rate_w,
        "Body mass (g)"                      as body_mass_g,
        "Temperature (K)"                    as temperature_k,
        "References"                         as references_
    from source
)

select * from renamed
